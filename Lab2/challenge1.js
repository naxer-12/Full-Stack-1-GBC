function greaterThanSize(){
    let randomSize = Math.floor(Math.random() * 100);
    let i=0
while(i <= randomSize){
    console.log("The current size is "+randomSize+" and count is "+i)
    i++;
}
}